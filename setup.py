from setuptools import setup, find_packages

setup(
    name="mindbox_lib",
    version="0.1",
    packages=find_packages(),
    description="Geometry library for area calculations",
    author="Your Name",
    install_requires=[],
    python_requires='>=3.7',
)
